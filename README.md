To use the template generator change the DAY variable in config.go to whatever day(crossword_id) you wanna refer to.
Then add the rows, columns and clues, to generate a template. 
By default the template is stores in the current wd. While deploying make sure to save it in Data/Day{DAY}
